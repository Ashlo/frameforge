{
  "name": "frameforge-demo-recorder",
  "version": "0.1.0",
  "private": true,
  "description": "Desktop-first demo recorder for short product walkthroughs",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test tests/*.test.mjs",
    "desktop:dev": "npm --prefix desktop run dev",
    "desktop:build": "npm --prefix desktop run build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "devDependencies": {
    "vite": "^5.4.19"
  }
}
